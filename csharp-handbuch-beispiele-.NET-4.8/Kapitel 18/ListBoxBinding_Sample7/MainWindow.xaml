<Window x:Class="ListBoxBinding_Sample7.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ListBoxBinding_Sample7"
        mc:Ignorable="d"
        Title="ListBoxBinding_Sample1" Height="300" Width="450">
    <Window.Resources>
        <local:PersonConverter x:Key="pConv" />
        <Style TargetType="Button">
            <Setter Property="Width" Value="100" />
            <Setter Property="Margin" Value="6" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="3 6 6 3" />
            <Setter Property="Grid.Column" Value="1" />
        </Style>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="ListBoxItem">
            <Setter Property="Margin" Value="1" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="Gray" />
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Foreground" Value="DarkBlue" />
                    <Setter Property="FontWeight" Value="Bold" />
                </Trigger>
            </Style.Triggers>

        </Style>
        <Style x:Key="blue" TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
            <Setter Property="Background" Value="LightBlue" />
        </Style>
        <Style x:Key="gray" TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
            <Setter Property="Background" Value="LightGray" />
        </Style>

        <local:ListBoxItemSelector x:Key="selector" 
                                   BlueStyle="{StaticResource blue}"
                                   GrayStyle="{StaticResource gray}" />

        <DataTemplate x:Key="default">
            <Grid Name="grid1">
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <TextBlock Name="tbName" Margin="4 0 0 0">
                    <TextBlock.Text>
                        <MultiBinding Converter="{StaticResource pConv}">
                            <Binding Path="LastName" />
                            <Binding Path="FirstName" />
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
                <TextBlock Margin="4 0 0 0" Name="tbAge" Grid.Row="1" Text="{Binding Age}" Foreground="Black"/>
                <TextBlock Margin="4 0 0 0" Name="tbCity" Grid.Row="2" Text="{Binding City}" Foreground="Black"/>
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="pension">
            <Grid Name="grid1" Background="DarkBlue">
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <TextBlock Name="tbName" Margin="4 0 0 0" Foreground="White">
                    <TextBlock.Text>
                        <MultiBinding Converter="{StaticResource pConv}">
                            <Binding Path="LastName" />
                            <Binding Path="FirstName" />
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
                <TextBlock Margin="4 0 0 0" Name="tbAge" Grid.Row="1" Text="{Binding Age}" Foreground="White"/>
                <TextBlock Margin="4 0 0 0" Name="tbCity" Grid.Row="2" Text="{Binding City}" Foreground="White"/>
            </Grid>
        </DataTemplate>

        <local:ListBoxDataSelector x:Key="templateSelector"
                                   DefaultTemplate="{StaticResource default}"
                                   PensionTemplate="{StaticResource pension}" />
    </Window.Resources>
    <Window.Background>
        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
            <GradientStop Color="Black" Offset="0"/>
            <GradientStop Color="White" Offset="1"/>
        </LinearGradientBrush>
    </Window.Background>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>


            <ListBox Name="listbox1" Margin="5" ItemContainerStyleSelector="{StaticResource selector}" HorizontalContentAlignment="Stretch" 
                     ItemTemplateSelector="{StaticResource templateSelector}" />

            <Grid Grid.Column="1" DataContext="{Binding SelectedItem, ElementName=listbox1}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Label Grid.Row="0">Vorname:</Label>
                <Label Grid.Row="1">Zuname:</Label>
                <Label Grid.Row="2">Alter:</Label>
                <Label Grid.Row="3">Stadt:</Label>
                <TextBox Grid.Row="0" Text="{Binding FirstName}" />
                <TextBox Grid.Row="1" Text="{Binding LastName}" />
                <TextBox Grid.Row="2" Text="{Binding Age}" />
                <TextBox Grid.Row="3" Text="{Binding City}" />
            </Grid>
        </Grid>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Button Name="btnAdd">Hinzufügen</Button>
            <Button Name="btnDelete">Löschen</Button>
        </StackPanel>
    </Grid>
</Window>
