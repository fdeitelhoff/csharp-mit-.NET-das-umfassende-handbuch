<Window x:Class="Navigation_Sample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Navigation_Sample"
        mc:Ignorable="d"
        Title="Navigation_Sample" Height="400" Width="600">
    <Window.Background>
        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
            <GradientStop Color="#FF1A133A" Offset="0"/>
            <GradientStop Color="White" Offset="1"/>
        </LinearGradientBrush>
    </Window.Background>

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="5" />
            <Setter Property="FontSize" Value="14" />
        </Style>
        <Style TargetType="Label">
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Height" Value="25" />
            <Setter Property="Width" Value="35" />
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Background="DarkGray">
            <Button Name="btnPrevious" Click="btnPrevious_Click">&lt;</Button>
            <TextBlock Foreground="White" Name="txtPosition" VerticalAlignment="Center" Width="140" Margin="5"/>
            <Button Name="btnNext" Click="btnNext_Click">&gt;</Button>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>
            <ListBox SelectionChanged="lstProducts_SelectionChanged" Name="lstProducts" Background="AliceBlue" 
                     Margin="5,5,4.8,4.8" ItemsSource="{Binding}" DisplayMemberPath="ProductName" />
            <Grid Grid.Column="1" Margin="0.2,0,-0.4,-0.2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Label Grid.Row="0">ProductName:</Label>
                <Label Grid.Row="1">UnitPrice:</Label>
                <Label Grid.Row="2">UnitsInStck:</Label>
                <Label Grid.Row="3">UnitsOnOrder:</Label>
                <TextBox Grid.Column="1" Grid.Row="0" Text="{Binding ProductName}" />
                <TextBox Grid.Column="1" Grid.Row="1" Text="{Binding UnitPrice}" />
                <TextBox Grid.Column="1" Grid.Row="2" Text="{Binding UnitsInStock}"/>
                <TextBox Grid.Column="1" Grid.Row="3" Text="{Binding UnitsOnOrder}"/>
            </Grid>
        </Grid>

    </Grid>
</Window>

