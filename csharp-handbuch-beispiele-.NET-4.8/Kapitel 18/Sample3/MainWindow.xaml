<Window x:Class="Sample3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Sample3"
        mc:Ignorable="d"
        Title="Sample3" Height="135" Width="300">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Height" Value="25" />
            <Setter Property="Width" Value="100" />
        </Style>
    </Window.Resources>
    <Window.Background>
        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
            <GradientStop Color="Black" Offset="0"/>
            <GradientStop Color="White" Offset="1"/>
        </LinearGradientBrush>
    </Window.Background>
    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <Button Command="ApplicationCommands.Copy" CommandTarget="{Binding ElementName=text1}">
                Kopieren
            </Button>
            <Button Command="ApplicationCommands.Cut" CommandTarget="{Binding ElementName=text1}">
                Ausschneiden
            </Button>
            <Button Command="ApplicationCommands.Paste" CommandTarget="{Binding ElementName=text1}">
                Einfügen
            </Button>
        </StackPanel>
        <TextBox Name="text1" Margin="5"/>
        <TextBox Name="text2" Margin="5, 0, 5, 5"/>
    </StackPanel>
</Window>

