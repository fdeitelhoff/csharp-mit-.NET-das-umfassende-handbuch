<Window x:Class="Converter_Sample1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Converter_Sample1"
        mc:Ignorable="d"
        Title="Converter_Sample1" Height="150" Width="220" FontSize="14">
    <Window.Resources>
        <local:ValueConverter x:Key="converter" />
        <local:Share x:Key="aktie" Company="Tollsoft" Value="211.73536"/>
    </Window.Resources>
    <Window.Background>
        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
            <GradientStop Color="Black" Offset="0"/>
            <GradientStop Color="White" Offset="1"/>
        </LinearGradientBrush>
    </Window.Background>

    <StackPanel>
        <TextBox Margin="10" Text="{Binding Value, Source={StaticResource aktie}, Converter={StaticResource converter}, ConverterParameter=#.##}" />
        <TextBox Margin="10" Text="{Binding Source={StaticResource aktie}, Path=Company}" />
    </StackPanel>
</Window>
