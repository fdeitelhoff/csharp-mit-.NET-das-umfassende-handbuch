<Window x:Class="MultiBinding_Sample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MultiBinding_Sample"
        mc:Ignorable="d"
        Title="MultiBinding_Sample" Height="100" Width="200">
    <Window.Resources>
        <local:PersonConverter x:Key="converter" />
        <local:Person x:Key="pers" Vorname="Peter" Zuname="Holzschuh" />
    </Window.Resources>
    <StackPanel>
        <TextBox Margin="10">
            <TextBox.Text>
                <MultiBinding Converter="{StaticResource converter}">
                    <Binding Source="{StaticResource pers}" Path="Zuname" />
                    <Binding Source="{StaticResource pers}" Path="Vorname" />
                </MultiBinding>
            </TextBox.Text>
        </TextBox>
    </StackPanel>
</Window>
