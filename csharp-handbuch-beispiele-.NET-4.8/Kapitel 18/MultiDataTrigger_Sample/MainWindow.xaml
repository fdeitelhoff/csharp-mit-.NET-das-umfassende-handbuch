<Window x:Class="MultiDataTrigger_Sample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MultiDataTrigger_Sample"
        mc:Ignorable="d"
        Title="MultiDataTrigger_Sample" Height="200" Width="350">
    <Window.Background>
        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
            <GradientStop Color="Black" Offset="0"/>
            <GradientStop Color="White" Offset="1"/>
        </LinearGradientBrush>
    </Window.Background>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBox Margin="5" FontWeight="Bold">Geschäftsbedingungen ....</TextBox>
        <CheckBox Grid.Row="1" Name="cb1" Margin="5" Content="Ich akzeptiere die allg. Geschäftsbedingungen" />
        <CheckBox Grid.Row="2" Name="cb2" Margin="5" Content="Ich bin mindestens 18 Jahre alt" />
        <Button Grid.Row="3" Margin="10" FontSize="12" Width="150" Click="Button_Click">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Content" Value="Angaben unvollständig" />
                    <Setter Property="IsEnabled" Value="False" />
                    <Style.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding ElementName=cb1, Path=IsChecked}" Value="true" />
                                <Condition Binding="{Binding ElementName=cb2, Path=IsChecked}" Value="true" />
                            </MultiDataTrigger.Conditions>
                            <Setter Property="IsEnabled" Value="true" />
                            <Setter Property="Content" Value="Bestellen" />
                        </MultiDataTrigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>
    </Grid>
</Window>
