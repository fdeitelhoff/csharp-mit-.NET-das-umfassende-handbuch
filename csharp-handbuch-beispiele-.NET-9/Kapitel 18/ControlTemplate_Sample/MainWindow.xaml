<Window x:Class="ControlTemplate_Sample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ControlTemplate_Sample"
        mc:Ignorable="d"
      Title="ControlTemplate_Sample" Height="250" Width="600">
    <Window.Resources>
        <ControlTemplate x:Key="roundedButton" TargetType="Button">
            <Grid>
                <Border BorderThickness="0" BorderBrush="DarkGray" CornerRadius="25" Name="border">
                    <Rectangle RadiusX="25" RadiusY="25" x:Name="rect">
                        <Rectangle.Fill>
                            <RadialGradientBrush>
                                <GradientStop Offset="0" Color="Wheat" />
                                <GradientStop Offset="1" Color="DarkGray" />
                            </RadialGradientBrush>
                        </Rectangle.Fill>
                    </Rectangle>
                </Border>
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"  />
            </Grid>
            
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="border" Property="BorderBrush" Value="Black" />
                    <Setter TargetName="border" Property="BorderThickness" Value="1" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter TargetName="rect" Property="Fill">
                        <Setter.Value>
                            <RadialGradientBrush>
                                <GradientStop Offset="0" Color="WhiteSmoke" />
                                <GradientStop Offset="1" Color="DarkGray" />
                            </RadialGradientBrush>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
        
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Button Template="{StaticResource roundedButton}" Margin="10">Cancel</Button>
        <Button Template="{StaticResource roundedButton}" Grid.Column="1" Height="70" Width="200">Ignore</Button>
    </Grid>
</Window>
