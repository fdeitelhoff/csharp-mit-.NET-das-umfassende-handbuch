<Window x:Class="ColorMixerTestApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ColorMixerTestApp"
        xmlns:mix="clr-namespace:ColorMixer_Sample;assembly=ColorMixer_Sample"
        mc:Ignorable="d"
     Title="ColorMixer" Height="260" Width="300">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Width" Value="90" />
            <Setter Property="Height" Value="25" />
            <Setter Property="Margin" Value="6 15 0 6" />
        </Style>
    </Window.Resources>
    <Window.Background>
        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
            <GradientStop Color="Black" Offset="0"/>
            <GradientStop Color="White" Offset="1"/>
        </LinearGradientBrush>
    </Window.Background>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <mix:ColorMixer x:Name="colorMixer" ColorChanged="colorMixer_ColorChanged" Margin="5" Padding="3" />
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Button Click="cmdGetColor_Click" Content="Farbe abrufen"/>
            <Button Click="cmdSet_Click" Content="Reset"/>
            <Button Command="Undo" CommandTarget="{Binding ElementName=colorMixer}" Content="Rückgängig"/>
        </StackPanel>
        <TextBlock Grid.Row="2" Width="170" FontSize="12" HorizontalAlignment="Left" x:Name="tblColor" Margin="10"/>
    </Grid>
</Window>
